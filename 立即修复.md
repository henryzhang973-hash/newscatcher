# ⚡ 立即修复认证问题

## 问题原因

GitHub 已不再支持密码认证，必须使用 **Personal Access Token**。

## 🚀 快速修复（3步）

### 步骤 1: 清除错误的凭据

运行以下命令：

```bash
cd /Users/henryzhang/TrendRadar/newscatcher

# 清除错误的凭据
echo "host=github.com
protocol=https" | git credential reject
```

或者运行修复脚本：

```bash
./快速修复认证.sh
```

### 步骤 2: 创建 Personal Access Token

1. 访问：https://github.com/settings/tokens
2. 点击 **"Generate new token"** → **"Generate new token (classic)"**
3. 填写：
   - **Note**: `NewsCatcher`
   - **Expiration**: 选择过期时间
   - **Select scopes**: ✅ 勾选 **`repo`**
4. 点击 **"Generate token"**
5. **立即复制 Token**（只显示一次！）

### 步骤 3: 使用 Token 推送

```bash
cd /Users/henryzhang/TrendRadar/newscatcher

# 推送代码
git push -u origin main
```

当提示输入时：
- **Username**: 你的 GitHub 用户名
- **Password**: **粘贴你的 Token**（不是密码！）

---

## 💡 更简单的方法：使用修复脚本

运行自动化修复脚本：

```bash
cd /Users/henryzhang/TrendRadar/newscatcher
./修复认证.sh
```

选择选项 2（使用 Personal Access Token），脚本会引导你完成所有步骤。

---

## 🔒 配置凭据助手（可选，方便后续使用）

推送成功后，配置凭据助手，以后就不需要每次都输入 Token：

```bash
git config --global credential.helper osxkeychain
```

下次推送时输入一次 Token，系统会记住。

---

## ❓ 如果还是不行

### 方法 A: 在 URL 中直接包含 Token

```bash
# 获取你的仓库地址（替换 USERNAME）
git remote set-url origin https://你的Token@github.com/你的用户名/newscatcher.git

# 推送
git push -u origin main
```

### 方法 B: 使用 SSH（最安全）

```bash
# 切换到 SSH
git remote set-url origin git@github.com:你的用户名/newscatcher.git

# 测试 SSH（如果还没配置，需要先配置 SSH key）
ssh -T git@github.com

# 推送
git push -u origin main
```

---

## ✅ 验证

推送成功后，你应该看到：

```
Enumerating objects: X, done.
Counting objects: 100% (X/X), done.
Writing objects: 100% (X/X), done.
To https://github.com/你的用户名/newscatcher.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

然后就可以去配置 GitHub Secrets 了！

