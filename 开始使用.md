# 🚀 NewsCatcher 快速开始

## 第一步：上传代码到 GitHub

### 方法一：使用脚本（推荐）

```bash
cd /Users/henryzhang/TrendRadar/newscatcher
./快速上传.sh
```

脚本会引导你完成：
- Git 用户配置
- 代码提交
- 连接 GitHub 仓库
- 推送代码

### 方法二：手动操作

#### 1. 在 GitHub 创建新仓库

访问：https://github.com/new

填写信息：
- Repository name: `newscatcher`
- Description: `AI新闻热点总结工具`
- 选择 Public 或 Private
- **不要**勾选 "Initialize this repository with a README"

点击 "Create repository"

#### 2. 配置 Git 并推送

```bash
cd /Users/henryzhang/TrendRadar/newscatcher

# 配置 Git 用户（如果还没配置）
git config user.name "你的GitHub用户名"
git config user.email "你的邮箱"

# 提交代码
git add .
git commit -m "Initial commit: NewsCatcher"

# 连接 GitHub 仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/newscatcher.git

# 推送代码
git branch -M main
git push -u origin main
```

**如果提示需要认证**：
- 使用 Personal Access Token（推荐）
- 或配置 SSH key
- 或使用 GitHub Desktop

---

## 第二步：配置 GitHub Secrets

### 快速配置清单

进入仓库：`Settings` → `Secrets and variables` → `Actions` → `New repository secret`

依次添加以下 Secrets：

| Secret 名称 | 值 | 说明 |
|------------|-----|------|
| `AI_API_KEY` | `sk-f7d071fdb38040ef890ee131fe7ff8d8` | 你的 DeepSeek API Key |
| `AI_BASE_URL` | `https://api.deepseek.com` | DeepSeek API 地址 |
| `AI_MODEL` | `deepseek-chat` | AI 模型名称 |
| `FEISHU_WEBHOOK_URL` | `你的飞书Webhook` | 飞书推送地址 |
| `TOP_N` | `10` | 每个平台抓取条数（可选） |

### 详细配置步骤

**详细步骤请查看**：`GitHub-Secrets配置指南.md`

**获取飞书 Webhook**：参考 `SETUP.md` 或 `上传指南.md`

---

## 第三步：测试运行

1. 进入仓库的 **Actions** 标签页
2. 找到 "News Summarizer" workflow
3. 点击 **"Run workflow"** → **"Run workflow"**
4. 等待运行完成（约 2-3 分钟）
5. 检查飞书是否收到消息

---

## 第四步：验证定时运行

程序已配置为每8小时自动运行：
- **UTC 时间**：0:00, 8:00, 16:00
- **北京时间**：8:00, 16:00, 0:00（次日）

你可以在 Actions 页面查看运行历史。

---

## 📚 相关文档

- `README.md` - 项目说明
- `SETUP.md` - 详细部署指南
- `上传指南.md` - 上传步骤
- `GitHub-Secrets配置指南.md` - Secrets 配置详解
- `项目说明.md` - 项目结构说明

---

## ❓ 遇到问题？

1. **查看 Actions 日志**：在 Actions 页面点击运行记录查看详细日志
2. **检查 Secrets**：确认所有必需的 Secrets 都已配置
3. **参考文档**：查看对应的详细指南文档

---

## 🎉 完成！

配置完成后，程序会自动每8小时运行一次，将 AI 总结推送到你的飞书。

享受自动化的新闻总结服务吧！ 📰✨

