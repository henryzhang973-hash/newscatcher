# Token 输入方法

## 🚀 最简单的方法：直接在命令行传入 Token

```bash
cd /Users/henryzhang/TrendRadar/newscatcher

# 直接在命令后加上你的 Token
./推送.sh 你的token在这里
```

**示例：**
```bash
./推送.sh ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

## 方法二：使用环境变量

```bash
# 设置环境变量
export GITHUB_TOKEN="你的token"

# 运行脚本
./推送.sh
```

---

## 方法三：保存到文件（最方便，但要注意安全）

```bash
# 创建 token 文件（只运行一次）
echo "你的token" > .github_token

# 添加到 .gitignore（确保不会提交到 GitHub）
echo ".github_token" >> .gitignore

# 运行脚本
./推送.sh
```

**⚠️ 安全提示：** `.github_token` 文件已添加到 `.gitignore`，不会被提交到 GitHub。

---

## 方法四：手动推送（不使用脚本）

```bash
cd /Users/henryzhang/TrendRadar/newscatcher

# 设置带 Token 的远程地址
git remote set-url origin https://你的token@github.com/henryzhang973-hash/newscatcher.git

# 推送
git push -u origin main

# 推送后清除 Token（安全考虑）
git remote set-url origin https://github.com/henryzhang973-hash/newscatcher.git
```

---

## 方法五：使用 GitHub CLI（如果已安装）

```bash
# 登录 GitHub
gh auth login

# 推送（会自动使用已登录的凭据）
git push -u origin main
```

---

## 📝 推荐流程

**第一次使用：**

```bash
cd /Users/henryzhang/TrendRadar/newscatcher

# 方式1：直接在命令行传入（最简单）
./推送.sh 你的token

# 或者方式2：保存到文件（方便后续使用）
echo "你的token" > .github_token
echo ".github_token" >> .gitignore
./推送.sh
```

**后续使用：**

如果使用了方式2（保存到文件），以后直接运行：
```bash
./推送.sh
```

---

## ❓ 如果还是无法输入

### 问题：终端不支持隐藏输入

**解决：** 使用文件方式或命令行参数方式

```bash
# 创建 token 文件
nano .github_token
# 粘贴你的 token，保存退出（Ctrl+X, Y, Enter）

# 添加到 gitignore
echo ".github_token" >> .gitignore

# 运行脚本
./推送.sh
```

### 问题：Token 包含特殊字符

**解决：** 使用引号包裹

```bash
./推送.sh "你的token（包含特殊字符）"
```

或者使用环境变量：
```bash
export GITHUB_TOKEN="你的token"
./推送.sh
```

---

## 🔑 获取 Token

如果还没有 Token：

1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token" → "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 生成并复制 Token

