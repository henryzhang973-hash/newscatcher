# 手动推送步骤（已创建仓库）

## 📋 完整操作步骤

### 步骤 1: 进入项目目录

```bash
cd /Users/henryzhang/TrendRadar/newscatcher
```

---

### 步骤 2: 配置 Git 用户信息（如果还没配置）

```bash
# 配置用户名
git config user.name "henryzhang973-hash"

# 配置邮箱
git config user.email "henryzhang973@gmail.com"
```

**检查配置：**
```bash
git config user.name
git config user.email
```

---

### 步骤 3: 设置远程仓库地址

```bash
# 设置远程仓库（替换为你的实际仓库地址）
git remote set-url origin https://github.com/henryzhang973-hash/newscatcher.git
```

**验证远程地址：**
```bash
git remote -v
```

应该看到：
```
origin  https://github.com/henryzhang973-hash/newscatcher.git (fetch)
origin  https://github.com/henryzhang973-hash/newscatcher.git (push)
```

---

### 步骤 4: 检查并提交代码

```bash
# 查看文件状态
git status

# 如果显示有未提交的文件，添加并提交
git add .
git commit -m "Initial commit: NewsCatcher - AI新闻总结工具"
```

**检查提交：**
```bash
git log --oneline -3
```

---

### 步骤 5: 推送代码到 GitHub

#### 方法 A: 使用 Token 推送（推荐）

```bash
# 获取你的 Personal Access Token
# 访问：https://github.com/settings/tokens

# 推送时使用 Token
git push -u origin main
```

**当提示输入时：**
- **Username**: `henryzhang973-hash`
- **Password**: **粘贴你的 Token**（不是密码！）

#### 方法 B: 在 URL 中直接包含 Token

```bash
# 设置带 Token 的远程地址（替换 YOUR_TOKEN 为你的实际 Token）
git remote set-url origin https://YOUR_TOKEN@github.com/henryzhang973-hash/newscatcher.git

# 推送
git push -u origin main

# 推送成功后，清除 URL 中的 Token（安全考虑）
git remote set-url origin https://github.com/henryzhang973-hash/newscatcher.git
```

#### 方法 C: 使用环境变量

```bash
# 设置环境变量
export GITHUB_TOKEN="你的token"

# 设置远程地址（包含 Token）
git remote set-url origin https://${GITHUB_TOKEN}@github.com/henryzhang973-hash/newscatcher.git

# 推送
git push -u origin main

# 清除 Token
git remote set-url origin https://github.com/henryzhang973-hash/newscatcher.git
unset GITHUB_TOKEN
```

---

## ✅ 验证推送成功

推送成功后，你应该看到类似输出：

```
Enumerating objects: X, done.
Counting objects: 100% (X/X), done.
Writing objects: 100% (X/X), done.
To https://github.com/henryzhang973-hash/newscatcher.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

然后访问：https://github.com/henryzhang973-hash/newscatcher 查看你的代码。

---

## 🔑 获取 Personal Access Token

如果还没有 Token：

1. 访问：https://github.com/settings/tokens
2. 点击 **"Generate new token"** → **"Generate new token (classic)"**
3. 填写：
   - **Note**: `NewsCatcher`
   - **Expiration**: 选择过期时间
   - **Select scopes**: ✅ 勾选 **`repo`**
4. 点击 **"Generate token"**
5. **立即复制 Token**（只显示一次！）

---

## 📝 完整命令序列（复制粘贴）

```bash
# 1. 进入目录
cd /Users/henryzhang/TrendRadar/newscatcher

# 2. 配置用户（如果还没配置）
git config user.name "henryzhang973-hash"
git config user.email "henryzhang973@gmail.com"

# 3. 设置远程仓库
git remote set-url origin https://github.com/henryzhang973-hash/newscatcher.git

# 4. 检查状态
git status

# 5. 如果有未提交的文件，提交
git add .
git commit -m "Initial commit: NewsCatcher"

# 6. 推送（会提示输入用户名和 Token）
git push -u origin main
# Username: henryzhang973-hash
# Password: 粘贴你的Token
```

---

## ❓ 常见问题

**Q: 提示 "repository not found"**  
A: 检查仓库名称是否正确，确保仓库已创建。

**Q: 提示 "Authentication failed"**  
A: 检查 Token 是否正确，是否有 `repo` 权限。

**Q: 提示 "Permission denied"**  
A: 确保 Token 有 `repo` 权限，或重新生成 Token。

**Q: 如何查看推送是否成功？**  
A: 访问 https://github.com/henryzhang973-hash/newscatcher 查看代码。

