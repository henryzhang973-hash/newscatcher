# NewsCatcher 上传到 GitHub 指南

## 📋 步骤 1: 在 GitHub 创建新仓库

1. 访问 https://github.com/new
2. 填写仓库信息：
   - **Repository name**: `newscatcher` (或你喜欢的名字)
   - **Description**: `AI新闻热点总结工具 - 自动抓取热点并推送到飞书`
   - **Visibility**: 选择 Public 或 Private
   - **不要**勾选 "Initialize this repository with a README"（我们已经有了）
3. 点击 "Create repository"

## 📋 步骤 2: 连接本地仓库并推送

在终端执行以下命令（替换 `你的用户名` 为你的 GitHub 用户名）：

```bash
cd /Users/henryzhang/TrendRadar/newscatcher

# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/newscatcher.git

# 或者使用 SSH（如果你配置了 SSH key）
# git remote add origin git@github.com:你的用户名/newscatcher.git

# 推送代码
git branch -M main
git push -u origin main
```

## 📋 步骤 3: 配置 GitHub Secrets

### 3.1 进入 Secrets 设置页面

1. 进入你的仓库页面：`https://github.com/你的用户名/newscatcher`
2. 点击 `Settings`（设置）
3. 在左侧菜单找到 `Secrets and variables` → `Actions`
4. 点击 `New repository secret`

### 3.2 添加必需的 Secrets

依次添加以下 Secrets（每个 Secret 点击一次 "New repository secret"）：

#### 1. AI_API_KEY
- **Name**: `AI_API_KEY`
- **Secret**: `sk-f7d071fdb38040ef890ee131fe7ff8d8`（你的 DeepSeek API Key）

#### 2. AI_BASE_URL
- **Name**: `AI_BASE_URL`
- **Secret**: `https://api.deepseek.com`

#### 3. AI_MODEL
- **Name**: `AI_MODEL`
- **Secret**: `deepseek-chat`

#### 4. FEISHU_WEBHOOK_URL
- **Name**: `FEISHU_WEBHOOK_URL`
- **Secret**: `你的飞书Webhook地址`（格式：`https://www.feishu.cn/flow/api/trigger-webhook/xxxxx`）

#### 5. TOP_N（可选）
- **Name**: `TOP_N`
- **Secret**: `10`（每个平台抓取条数，默认10）

### 3.3 验证 Secrets

添加完成后，你应该看到以下 Secrets 列表：

```
✅ AI_API_KEY
✅ AI_BASE_URL
✅ AI_MODEL
✅ FEISHU_WEBHOOK_URL
✅ TOP_N (可选)
```

## 📋 步骤 4: 测试运行

1. 进入仓库的 `Actions` 标签页
2. 找到 "News Summarizer" workflow
3. 点击 "Run workflow" → "Run workflow"
4. 等待运行完成（约 2-3 分钟）
5. 检查飞书是否收到消息

## 📋 步骤 5: 验证定时运行

Workflow 已配置为每8小时自动运行一次：
- UTC 时间：0:00, 8:00, 16:00
- 北京时间：8:00, 16:00, 0:00（次日）

你可以在 Actions 页面查看运行历史。

## ❓ 常见问题

### Q: 如何获取飞书 Webhook？

A: 详细步骤见 `SETUP.md` 文件，或参考主项目 README.md 的飞书配置部分。

### Q: 推送代码时提示需要认证？

A: 
- 如果使用 HTTPS：需要配置 Personal Access Token
- 如果使用 SSH：需要配置 SSH key
- 或者使用 GitHub Desktop 等工具

### Q: 如何修改运行频率？

A: 编辑 `.github/workflows/news-summarizer.yml`，修改 cron 表达式。

### Q: 如何查看运行日志？

A: 在 Actions 页面点击对应的 workflow run，查看详细日志。

## 🎉 完成！

配置完成后，程序会自动每8小时运行一次，将 AI 总结推送到你的飞书。

